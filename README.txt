My miscellaneous somewhat reusable backup scripts.

- `thin-backups.rb` removes old daily backups. Can be configured to e.g. keep every other backup that is over two weeks old and to keep only weekly backups when they are over a month old.
